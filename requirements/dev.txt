# Responsibility: Pin the developer and CI toolchain - what lints, type-checks and builds the distribution.
# Boundaries: none of it ships in a runtime image; runtime and test dependencies live in runtime.txt.

# requirements/dev.txt - the CANONICAL developer + CI toolchain.
#
# OWNERSHIP: HAND-EDITED (like requirements/runtime.txt - no generated locks anywhere).
# The authoritative pins for tools that are NOT part of any runtime environment: they lint,
# type-check and build the distribution, and must never ship in an image.
#
# These once lived inline in .github/workflows/ci.yml (`pip install ruff==… mypy==…`), which
# made CI a second, independently-editable dependency list. One file now, read by CI, by
# `make setup`, and by developers. `make dependencies` validates there is no drift.
#
#   pip install -c requirements/constraints.txt -r requirements/runtime.txt -r requirements/dev.txt
#
# Runtime + test dependencies (including pytest) are in requirements/runtime.txt.
# See "Dependencies" in docs/development/overview.md.

ruff==0.16.4
mypy==2.3.1

# Randomised test ORDER - surfaces inter-test state leakage that a fixed order hides. Auto-active
# once installed; it prints the seed so any failure is reproducible with --randomly-seed=<n>.
# `make test-random` runs a fixed seed matrix; CI pins one deterministic seed.
pytest-randomly==4.1.0

# PEP 517 frontend - builds the wheel in the Dockerfile's `wheel` stage and locally.
build==1.5.0
