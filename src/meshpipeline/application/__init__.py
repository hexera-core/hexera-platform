# Responsibility: Define the application boundary - the use cases that sit between the HTTP surface and adapters.
# Boundaries: no code on purpose; nothing is re-exported, so a caller imports the use case it invokes.
