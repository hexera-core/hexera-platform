# Responsibility: Define the package holding model inference - the provider clients, routing, streaming and tracing.
# Boundaries: no code on purpose - a caller imports the provider or the router by name.
