# Responsibility: Define the package holding the model-capacity contract's two implementations, in-process and Redis.
# Boundaries: no code on purpose - the composition root imports the implementation it binds.
