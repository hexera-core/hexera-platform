# Responsibility: Define the package holding the firebase-token contract's implementation, Google Identity Platform.
# Boundaries: no code on purpose - the composition root imports the implementation it binds.
