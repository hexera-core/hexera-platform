# Responsibility: Define the package holding the ws-ticket contract's two implementations, in-memory and Redis.
# Boundaries: no code on purpose - the composition root imports the implementation it binds.
