# Responsibility: Define the package holding the mesh-execution implementations - Cloud Run, its exchange, in-process.
# Boundaries: no code on purpose - the composition root imports the executor it binds.
