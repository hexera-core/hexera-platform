# Responsibility: Define the agents boundary - intake, builder and reviewer, plus the loop machinery they share.
# Boundaries: no code on purpose - importing it starts no agent and reads no settings.
