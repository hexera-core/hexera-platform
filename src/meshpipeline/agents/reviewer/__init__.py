# Responsibility: Define the package holding the reviewer agent - its evidence, eligibility, render runtime and tools.
# Boundaries: no code on purpose; nothing is re-exported, so each collaborator imports the module it uses.
