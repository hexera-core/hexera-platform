# Responsibility: Define the package holding the intake agent - its turns, approval gate and engine selection.
# Boundaries: no code on purpose; nothing is re-exported, so each collaborator imports the module it uses.
