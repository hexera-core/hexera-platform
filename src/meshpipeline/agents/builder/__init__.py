# Responsibility: Define the package holding the builder agent - its loop policy, tools, context and executor.
# Boundaries: no code on purpose; nothing is re-exported, so each collaborator imports the module it uses.
