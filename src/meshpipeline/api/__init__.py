# Responsibility: Define the HTTP boundary - the application, its middleware, its schemas and the versioned routes.
# Boundaries: no code on purpose - importing it must not construct the application.
