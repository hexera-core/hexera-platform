# Responsibility: Define the package holding one repository per aggregate - jobs, artifacts, geometry, sessions, outbox.
# Boundaries: no code on purpose - a caller imports the repository it reads or writes.
