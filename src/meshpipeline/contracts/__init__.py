# Responsibility: Define the contracts boundary; each module states one port the rest of the system depends on.
# Boundaries: no code on purpose - a contract is imported from its own module, never from here.
