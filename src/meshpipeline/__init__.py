# Responsibility: Mark the distribution's root package and state the version it reports.
# Boundaries: the version string only - importing the package pulls in no submodule.
__version__ = "0.1"
__all__ = ["__version__"]
