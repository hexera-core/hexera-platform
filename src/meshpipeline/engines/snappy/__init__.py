# Responsibility: Define the snappyHexMesh bundle boundary - its planner, spec, drivers, gates and review renderer.
# Boundaries: no code on purpose - the registry resolves the bundle through spec.py.
