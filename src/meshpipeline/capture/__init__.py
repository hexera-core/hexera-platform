# Responsibility: Define the package holding data capture - the scope a run is bound to and the records written.
# Boundaries: no code on purpose - importing it neither opens a scope nor enables collection.
